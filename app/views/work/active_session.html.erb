<%= render partial: "normal_locale" %>

<%= react_component('FloorView', {:defaultIconSet => ['', asset_path('default_iconset/bad.png'), asset_path('default_iconset/closed.png'), asset_path('default_iconset/like.png'), asset_path('default_iconset/question.png')], :buildingData => @building_data.as_json(:include => {:building => {:only => [:id], :methods => :ordered_floors, :include => {:floors => {:only => [:id, :name, :seq], :include => {:floor_objects => {:only => [:id, :x, :y, :width, :height, :direction, :label, :object_type]}}}}}}, :only => [:id]), :workLabel => @session_data['name'], :normalModeStoreKey => '_s'+@session_data['id'].to_s, :id => params[:id], :u => user_signed_in?, :shareableLink => @shareable_link}) %>